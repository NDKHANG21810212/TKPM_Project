package com.example.TKPM_Project.repository;
import com.example.TKPM_Project.model.GuestResult;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GuestResultRepository extends JpaRepository<GuestResult, Integer> {
}